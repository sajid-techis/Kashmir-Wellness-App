export enum Role {
  User = 'user',
  Admin = 'admin',
  Doctor = 'doctor',
  LabAdmin = 'lab_admin',
  HospitalAdmin = 'hospital_admin',
  Provider = 'provider', // This can be used for any provider type like doctor, lab, or hospital
  // Add any other roles you might need
}